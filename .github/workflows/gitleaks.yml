# gitleaks — secret-scanning gate. Canonical logic lives in the shared reusable workflow;
# this file is a thin pinned caller. Check-run: `gitleaks / gitleaks`.
name: gitleaks
on:
  pull_request:
  push:
    branches: [main]
permissions:
  contents: read
jobs:
  gitleaks:
    uses: Octet-Public/octet-workflows/.github/workflows/gitleaks.yml@5de5fbb2ffc86374ee2ffdb2a2beb5c0b966af0c  # v2.6.4
