# gitleaks — secret-scanning gate. Canonical logic lives in the shared reusable workflow;
# this file is a thin pinned caller. Check-run: `gitleaks / gitleaks`.
name: gitleaks
on:
  pull_request:
  push:
    branches: [main]
permissions:
  contents: read
jobs:
  gitleaks:
    uses: Octet-Public/octet-workflows/.github/workflows/gitleaks.yml@d7a0c4b0cec6e441ed10d89488487f8a368759b0  # v2.8.3
